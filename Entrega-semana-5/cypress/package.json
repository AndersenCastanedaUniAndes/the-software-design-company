{
  "name": "cypress-e2e-test",
  "version": "1.0.0",
  "description": "end to end test with cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "./node_modules/cypress/bin/cypress open",
    "run:headless": "./node_modules/cypress/bin/cypress run --headless",
    "run:reporter": "./node_modules/cypress/bin/cypress run --reporter cypress-mochawesome-reporter"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.2.0",
    "cypress": "^13.5.0",
    "cypress-mochawesome-reporter": "^3.6.1"
  },
  "dependencies": {
    "@faker-js/faker": "^8.2.0"
  }
}
